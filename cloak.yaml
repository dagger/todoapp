name: todoapp
workflows:
  - path: scripts/deploy-1
    sdk: ts
  - path: scripts/deploy-2
    sdk: ts
  - path: api/magefiles
    sdk: go
dependencies:
  - git:
      remote: git@github.com:dagger/cloak.git
      ref: demov2
      path: examples/yarn/cloak.yaml
  - git:
      remote: git@github.com:dagger/cloak.git
      ref: demov2
      path: examples/netlify/ts/cloak.yaml


name: todoapp
scripts:
  - path: scripts/kick-the-tires
    sdk: ts
  - path: scripts/deploy
    sdk: ts
  - path: api/magefiles
    sdk: go
dependencies:
  - git:
      remote: git@github.com:dagger/cloak.git
      ref: main
      path: examples/yarn/cloak.yaml
  - git:
      remote: git@github.com:dagger/cloak.git
      ref: main
      path: examples/netlify/ts/cloak.yaml
extensions:
  - path: extensions/deploy
    sdk: ts
